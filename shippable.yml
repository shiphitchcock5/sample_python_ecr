language: python

python:
  - 3.4

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE


    
build:
 # pre_ci:
  #  - docker build -t=shippabledocker/hitchcock2_sample_python .

  ci:
    - ps -eax
    - ls -al

  post_ci:
    - docker build -t=gcr.io/vidya-project/newimg3.
    - docker push gcr.io/vidya-project/newimg3
    

integrations:
  hub:
    - integrationName: ship-gcr
      type: gcr
      
